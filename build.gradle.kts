// Top-level build file (minimal). Module build file moved to `app/build.gradle.kts`.

// Repository configuration is defined in settings.gradle.kts via
// `dependencyResolutionManagement` (repositoriesMode). Do not declare
// repositories here to avoid conflicts with the settings-level policy.

